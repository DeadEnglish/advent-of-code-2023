import { readFile } from "../../helpers/readFile.helper";

const puzzle = readFile("day-9").split("\n");
const solutionOne = () => {
	return "todo";
};

const solutionTwo = () => {
	return "todo";
};

export const dayNineAnswers = () => {
	return {
		solutionOne: solutionOne(),
		solutionTwo: solutionTwo(),
	};
};
